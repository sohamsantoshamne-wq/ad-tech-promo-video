# Old voice-take backups from iterating on the narration — not needed, only
# the current assets/audio/scene_*.wav files matter (used by final.mp4)
assets/audio/backup_*/

# Duplicate copies of the same video — final.mp4 at the project root is canonical
deliverables/*.mp4

# Python
__pycache__/
*.pyc
